# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native — generated by prebuild, not source. Same reason we kept running
# `rm -rf ios` during setup: these are build output, not something to track.
/ios
/android

# env — real values, never committed (matches the root .env.local convention)
.env
.env*.local

# macOS
.DS_Store

# Metro
.metro-health-check*

# typescript
*.tsbuildinfo

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli